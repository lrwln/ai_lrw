# Ajax

## JSON.stringify(value,replace?,space?)
- 将对象序列化为 JSON 字符串
- replace 取舍？ null代表原样数据化，函数代表自定义序列化规则，数组代表只序列化数组中指定的属性
- space 用于美化输出的字符串，数字表示缩进空格数，字符串表示每个级别的缩进字符串(团队规范 可读性)

## js异步处理
- 回调函数：函数作为参数传递给另一个函数，在特定事件发生时被调用
- Promise(封装异步任务)+then
- async/await(基于Promise的语法糖，提供更简洁的异步代码写法)




